{
  "extends": "solhint:recommended",
  "rules": {
    "compiler-version": ["warn", "^0.8.16"],
    "func-visibility": ["warn", { "ignoreConstructors": true }],
    "mark-callable-contracts": "off",
    "avoid-low-level-calls": "warn"
  }
}
